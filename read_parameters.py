#!bin/python
#-*-coding:utf-8-*-

modele = []
fic = open("parameters.txt","r")
lines = fic.read().split("\n")
l = 0
while l < len(lines) :
    if len(lines[l]) != 0 and lines[l][0] != "/" :
        if lines[l][0] == "*" :
            while 
    l+=1 

fic.close()


# name="toto" 
# f=file('photo/'%s'.jpeg', 'wb') % name 

# f=file('photo/%s.jpeg' % name, 'wb') 
 
#création de classes pour chaque objet 